@using System.Globalization
@model IEnumerable<ProductoViewModel>

<h2>Listado de Productos</h2>

<table class="table">
    <thead>
        <tr>
            <th>Nombre</th>
            <th>Descripción</th>
            <th>Costo</th>
            <th>Precio</th>
            <th>Stock Disponible</th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var producto in Model)
        {
            <tr>
                <td>@producto.NombreProducto</td>
                <td>@producto.Descripcion</td>
                <td>AR$ @producto.Costo.ToString("N2", new CultureInfo("es-AR"))</td>
                <td>AR$ @producto.Precio.ToString("N2", new CultureInfo("es-AR"))</td>
                <td>@producto.StockDisponible</td>
                <td>
                    <a asp-action="Edit" asp-route-id="@producto.ProductoId" class="btn btn-primary">Editar</a>
                    <a asp-action="Delete" asp-route-id="@producto.ProductoId" class="btn btn-danger">Eliminar</a>
                </td>
            </tr>
        }
    </tbody>
</table>

<a asp-action="Create" class="btn btn-success">Nuevo Producto</a>
